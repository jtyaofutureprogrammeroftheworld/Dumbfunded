<%= form_for(dare) do |f| %>
  <% if dare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dare.errors.count, "error") %> prohibited this dare from being saved:</h2>

      <ul>
      <% dare.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.text_field :message, placeholder: "What's happening?", 
    class: "form-control" %>
  </div>

  <div class="field">
    <%= f.hidden_field :user_id, value: current_user.id %>
  </div>

  <div class="form-group">
    <%= f.submit "just do it!", class: "btn btn-primary" %>
  </div>


<% end %>
